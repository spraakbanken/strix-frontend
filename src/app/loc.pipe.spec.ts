/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LocPipe } from './loc.pipe';

describe('Pipe: Loc', () => {
  it('create an instance', () => {
    let pipe = new LocPipe();
    expect(pipe).toBeTruthy();
  });
});
